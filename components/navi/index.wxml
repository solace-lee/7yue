<!--components/navi/index.wxml-->
<view class="container">
  <image class="img" bind:tap="onLeft" src="{{laste ? disLeftSrc : leftSrc}}" />
  <text class="title">{{title}}</text>
  <image class="img" bind:tap="onRight" src="{{first ? disRightSrc : rightSrc}}" />
</view>